t=find_task, args=(txt + "." + k ,v,))
            #     thread_items.append(t)
            # for t in thread_items:
            #     t.start()
            # for t in thread_items:
            #     t.join()